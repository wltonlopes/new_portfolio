// import { render, screen } from '@testing-library/react';
// import App from './App';

// test('Verificando se no app possui o texto', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/welton/i);
//   expect(linkElement).toBeInTheDocument();
// });
